"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance"); }

function _iterableToArray(iter) { if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } }

var _default = function _default(s) {
  var _s$slice;

  var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  var _options$start = options.start,
      start = _options$start === void 0 ? '' : _options$start,
      end = options.end,
      _options$replacer = options.replacer,
      replacer = _options$replacer === void 0 ? [] : _options$replacer;
  var endIndex = end ? s.indexOf(end) : undefined;
  var startIndex = start ? s.indexOf(start) + start.length : 0;
  return (_s$slice = s.slice(startIndex, endIndex)).replace.apply(_s$slice, _toConsumableArray(replacer)).trim();
};

exports.default = _default;